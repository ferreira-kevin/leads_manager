import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#ff7b19',
    },
    secondary: {
      main: '#eeeeee',
    },
    background: {
      default: '#eeeeee',
    },
  },
});

export default theme;
